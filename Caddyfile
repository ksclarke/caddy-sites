#
# Enables the admin API (Can only be accessed locally because of Docker's port mapping)
#
{
        admin 0.0.0.0:9999
}

#
# Proxies the jpv3-webrepl service running on this same machine (and handles TLS)
#
jpv3.lisforge.net {
	reverse_proxy http://webrepl:8888
}

#
# Proxies the audiobooks service running on this same machine (and handles TLS)
#
audiobooks.kevinclarke.info {
	reverse_proxy http://audiobooks:80
}

#
# Proxies the music service running on this same machine (and handles TLS)
#
music.kevinclarke.info {
        reverse_proxy http://music:4533
}

#
# Proxies the ebooks service running on this same machine (and handles TLS)
#
ebooks.kevinclarke.info {
        reverse_proxy http://ebooks:5000
}

#
# Proxy the weblog running on this same machine (and handles TLS)
#
weblog.kevinclarke.info {
    root * /usr/share/caddy
    file_server
}
