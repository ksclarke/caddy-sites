
This file should be in the `.config` directory in the user's home directory. In this repo, the `.config` directory is
represented by a `config` directory since I don't want the directory to be hidden.
